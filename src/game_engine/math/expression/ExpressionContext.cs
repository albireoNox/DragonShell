namespace game_engine.math.expression
{
    // Contains data that expression will need when they are evaluated (e.g. references to some entity with stats)
    // TODO: Implement!
    public interface ExpressionContext
    {
    }
}
